# robots.txt for phind.us

# Allow all search engines to crawl the site
User-agent: *
Allow: /

# Disallow crawling of API endpoints and backend
Disallow: /api/
Disallow: /backend/

# Sitemap location
Sitemap: https://phind.us/sitemap.xml

# Crawl delay (helps prevent server overload)
Crawl-delay: 1
